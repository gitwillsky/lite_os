[workspace]
members = [
    "kernel",
    "syscall-abi",
    "tools/architecture-check",
    "tools/kernel-unit",
    "tools/scheduler-unit",
]
exclude = ["bootloader", "user/display-client", "user/display-session", "user/liteos-2d", "user/liteos-terminal"]
default-members = ["kernel"]
resolver = "3"

[profile.release]
panic = "abort"
opt-level = "s"
lto = "fat"

[profile.dev]
panic = "abort"
opt-level = "s"
debug-assertions = true
