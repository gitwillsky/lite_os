# LiteOS 工程指南

当前仓库事实、编码约束、构建命令和验证方式以 `AGENTS.md` 为唯一权威。详细架构见 `docs/architecture.md`，syscall 状态见 `docs/syscall-support.md`，固定规范见 `docs/standards-baseline.md`。

不得根据历史描述恢复已删除的 fork/signal/futex/IPC/fd/GUI/shell 等功能，也不得声称 LiteOS 当前完整兼容 Linux、POSIX 或 musl。
