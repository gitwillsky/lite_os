[package]
name = "display-session"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
service-activation = { path = "../service-activation" }

[profile.release]
panic = "abort"
opt-level = "s"
lto = "fat"
codegen-units = 1
