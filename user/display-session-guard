#!/bin/sh

# display-session 是 seat0 capability transition 的唯一 owner；它一旦退出，旧设备
# 是否仍被 guest process 持有便无法在 userspace 证明，继续运行会形成第二个 owner。
/bin/display-session
status=$?
echo "display-session exited (${status}); cold rebooting" >&2
sync
reboot -f
exit 125
