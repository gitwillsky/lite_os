[package]
name = "liteos-terminal"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
display-client = { path = "../display-client" }

[profile.release]
panic = "abort"
opt-level = "s"
lto = "fat"
codegen-units = 1
