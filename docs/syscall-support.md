# LiteOS Linux/riscv64 syscall 支持矩阵

> 更新日期：2026-07-15（Asia/Shanghai）
>
> Linux UAPI 基线：Linux `v7.1`，commit `8cd9520d35a6c38db6567e97dd93b1f11f185dc6`
>
> POSIX 语义基线：POSIX.1-2024 / Issue 8
>
> musl consumer 基线：musl `v1.2.6`，commit `9fa28ece75d8a2191de7c5bb53bed224c5947417`

## 领域索引

| 领域 | 权威文件 | 覆盖范围 |
|---|---|---|
| Socket ABI | [syscall/socket.md](syscall/socket.md) | AF_UNIX、AF_INET、AF_PACKET、AF_NETLINK 的 message、backpressure、poll、option 与缺口 |
| 其余 syscall | 本文件矩阵 | 编号、状态、consumer、errno 与实现位置；已迁移领域只保留索引和状态摘要，禁止复制长 contract |

## 1. 全局契约

- U-mode `ecall`：`a7=number`，`a0..a5=args`，`a0=result`。
- kernel error 为 `-errno`；user raw wrapper 不伪造 libc `errno`。
- `syscall-abi` 只定义下表 140 个 Linux/riscv64 number。
- dispatcher 对所有其他 number 安静地返回 `-ENOSYS`；正常 capability/io_uring 等 userspace probe 不得放大为逐调用 kernel log。
- 没有 LiteOS 私有 syscall number、旧编号转发或 feature-flag 双轨；固定 consumer 必需的 legacy `tkill` 只增加标准 ABI selector，不复制 signal implementation。

状态含义：

| 状态 | 含义 |
|---|---|
| Complete | 在当前明确声明的 process/thread/fd 对象模型内，无已知的本 syscall 契约偏差。 |
| Partial | 入口可用，但已知缺失在表中精确列出。 |
| Missing | 尚无正确实现；当前 `-ENOSYS`。 |
| Not Planned | 当前收敛基线不计划接入；当前 `-ENOSYS`。 |
| Removed | 曾有错号、私有或语义不完整实现，已整链删除；当前 `-ENOSYS`。 |

`Complete` 不能外推为完整 Linux/POSIX/musl 兼容。例如 `set_tid_address` 的 clear/wake 契约成立，不表示 futex PI/requeue、所有 syscall 的 restart 或完整 pthread runtime 已成立。

## 2. 当前暴露的 140 个入口

| 编号 | Linux 名称 | 参数 / userspace ABI | 返回与 errno | POSIX / musl 路径 | 状态与精确边界 | 代码 |
|---:|---|---|---|---|---|---|
| 17 | `getcwd` | `char *buf, size_t size` | 含 NUL 的长度；`ERANGE/EFAULT/ENOENT` | POSIX `getcwd`；musl direct wrapper | **Complete**（当前单 root namespace）。从 Process 持有的 VFS opened-entry parent 链生成 raw absolute path，不缓存字符串；ancestor rename 立即可见，任一链节点已删除返回 `ENOENT`。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/vfs.rs` |
| 19 | `eventfd2` | initial counter、`EFD_CLOEXEC/NONBLOCK/SEMAPHORE` | fd；`EAGAIN/EBADF/EFAULT/EINVAL/EMFILE` | Linux eventfd；curl/c-ares/libuv | **Complete**（当前 fd/poll 模型）。单一 64-bit counter owner 支持普通累加消费与 semaphore 单步消费、blocking/nonblocking、overflow backpressure、fork/dup/exec/close，以及 pselect/ppoll/epoll 的读写 readiness 与 edge generation；读写方向各用一条独立的一字节 notification Pipe 接入统一 wait seam，不复制 counter。scalar/readv 的总 capacity 至少 8 bytes 并只返回一个 u64，writev 按 scalar fallback 逐个非空 iovec 写入；写 `UINT64_MAX` 返回 `EINVAL`。 | `kernel/src/ipc/eventfd.rs`, `kernel/src/syscall/eventfd.rs`, `kernel/src/syscall/fs/io/sequential/read.rs`, `kernel/src/syscall/fs/io/sequential/write.rs` |
| 20-22 | `epoll_create1/epoll_ctl/epoll_pwait` | Linux RV64 epoll event ABI | ready count；标准 errno | Linux epoll；musl event loop | **Partial**。当前 pollable OFD（含 evdev/PTY/DRM）支持 LT/ET/ONESHOT、PRI/RDHUP、`EPOLLEXCLUSIVE` source-filtered wake-one、5 层无环嵌套、ready-list 轮转、timeout/temporary signal mask、跨 fork 最后 descriptor close、`(fd,OFD)` reuse identity 与并发 close/MOD/delivery revision；普通与 exclusive 共存时，每个普通 epoll instance 唤醒一个 thread，全部 exclusive instance 合计只唤醒一个。一字节 notification Pipe 的 generation 只承载 edge，最终 readiness 始终复查 backend level state；PTY peer close 无条件发布 HUP。无 suspend policy 时按 Linux 非特权语义忽略 `EPOLLWAKEUP`；当前无 source 的 `RDNORM/RDBAND/WRNORM/WRBAND/MSG` 扩展位仍不开放。 | `kernel/src/syscall/epoll.rs`, `kernel/src/fs/epoll.rs`, `kernel/src/syscall/poll.rs`, `kernel/src/task/task_manager/wait_registry.rs` |
| 23/24 | `dup` / `dup3` | fd、目标 fd、`O_CLOEXEC` | 新 fd；`EBADF/EINVAL` | POSIX dup；Linux dup3 | **Complete**。fd entry 复制后共享同一 OFD offset/status flags；descriptor flag 独立。dup/F_DUPFD 通过 fd-table 四层 free-slot index 返回最低合格 fd，不扫描 entry vector；dup3 原子发布目标 entry，被替换 descriptor 在 files lock 外完成最后引用与 epoll/flock/record-lock cleanup。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/file/descriptor_table.rs` |
| 25 | `fcntl` | fd、command、argument 或 RV64 32-byte `struct flock` | command 对应值；`EBADF/EAGAIN/EFAULT/EINTR/EINVAL/ENOLCK/EOVERFLOW` | POSIX fcntl；SQLite | **Partial**。实现 descriptor/status commands 与 `F_GETLK/F_SETLK/F_SETLKW` process-associated byte-range locks；VFS 以 mounted inode + TGID 唯一持有拆分/合并后的 range，任一同 inode descriptor close 或 Process exit 释放，blocking wait 可被 signal 中断并按统一 restart policy 重放。`F_SETFL` 只修改 `O_APPEND/O_NONBLOCK`；OFD locks、lease/owner/pipe-size commands 尚未开放。 | `kernel/src/syscall/fs/fcntl.rs`, `kernel/src/fs/vfs/record_lock.rs`, `kernel/src/task/task_manager/advisory_lock.rs` |
| 29 | `ioctl` | fd、request、request-specific argument | fixed request 返回 0，variable evdev query 返回 byte count；标准 TTY/socket/DRM/evdev errno | musl termios；BusyBox init/ash/stty/ifconfig/route；htop ncurses；DRM/evdev/PTY clients | **Partial**。TTY 与 AF_INET interface/route 保持既有范围；Unix98 master 支持 TIOCGPTN/TIOCSPTLCK，通用 termios/session/window ioctl 投影到同一 slave Terminal，成功 TIOCSCTTY 同时替换 Process 的 `/dev/tty` handle；TIOCSWINSZ 只在 8-byte winsize 改变时提交并向 foreground group 发送 kernel `SIGWINCH`。DRM query、dumb/KMS/event 支持 connector preferred mode 与 active CRTC mode 的独立快照；DIRTYFB 按 Linux 24-byte cmd mask annotation flags、验证 copy pairs 与 pointer/count，一次最多 32 clips，空列表 flush full framebuffer，并阻塞到 TRANSFER+FLUSH completion。evdev 保持既有 input state 范围。DRM atomic/auth/lease/vblank-wait、TIOCGPTPEER/packet mode 与 evdev output/revoke/mask/multitouch-slot/hotplug 尚未开放；其他缺口仍包括 TCSETSW drain、TCSETSF flush、TIOCNOTTY、rtnetlink 与多 interface。 | `kernel/src/syscall/ioctl.rs`, `kernel/src/syscall/{tty,drm,input}.rs`, `kernel/src/syscall/socket/interface.rs`, `kernel/src/{drm,input}.rs`, `kernel/src/fs/pty.rs` |
| 30/31 | `ioprio_set/ioprio_get` | `IOPRIO_WHO_PROCESS`、TID、encoded class/data | 0/priority；`EINVAL/EPERM/ESRCH` | Linux task I/O policy；htop | **Partial（per-Thread process selector）**。零选择 caller、正数选择全局 live TID；支持 NONE/RT/BE/IDLE class 与 data 0..7，policy 由 SchedulingEntity lock 唯一拥有，fork/clone 继承、exec 保留。set 只允许相同 scheduler identity 或 root。尚未实现 PGRP/USER collection 与 block scheduler enforcement。 | `kernel/src/syscall/scheduler.rs`, `kernel/src/task/task_manager/policy.rs`, `kernel/src/task/model/scheduling.rs` |
| 32 | `flock` | fd、`LOCK_SH/EX/UN[\|LOCK_NB]` | 0；`EBADF/EINVAL/EINTR/EWOULDBLOCK/ENOLCK/EOPNOTSUPP` | BSD whole-file lock；apk-tools database serialization | **Complete（当前 pathname-backed OFD scope）**。VFS 以 mounted inode identity 唯一拥有 shared/exclusive lock table；lock identity 属于 OFD，dup/fork 共享、独立 open 互斥、exec 保留、显式 unlock 或最后 descriptor close 释放。转换先释放旧模式；blocking wait 经统一 interruptible registry 封闭 lost-wakeup race并支持 `SA_RESTART`。anonymous pipe/socket/epoll 尚无 VFS inode，明确返回 `EOPNOTSUPP`。 | `kernel/src/fs/vfs/advisory_lock.rs`, `kernel/src/task/task_manager/advisory_lock.rs`, `kernel/src/syscall/fs/flock.rs` |
| 33 | `mknodat` | dirfd、pathname、mode、device | 0；标准 pathname/permission errno；未支持类型返回 `EOPNOTSUPP` | musl mknod；apk-tools package extraction | **Partial（普通文件 scope）**。type 为零或 `S_IFREG` 时复用 VFS 的 parent permission、umask、setgid inheritance 与 ext2 journal mutation；FIFO/socket/character/block device node 尚无完整 persistent object/open 语义，不伪造创建成功。 | `kernel/src/syscall/fs/namespace.rs`, `kernel/src/fs/vfs/mutation.rs` |
| 34-37 | `mkdirat/unlinkat/symlinkat/linkat` | dirfd、raw path/target、mode/flags | 0；标准 pathname/link errno，ext2 link ceiling 返回 `EMLINK` | POSIX namespace mutation；musl；BusyBox mkdir/rm/ln | **Complete**（当前单 mount namespace）。VFS 统一执行 parent write+search、umask、setgid inheritance、sticky directory 与 protected hardlink policy；所有 ext2 mutation 经同一 journal transaction。hardlink target 与 mkdir parent 的正 link delta 在固定 32,000 上限前拒绝，mkdir 的 `EMLINK` 位于 inode/entry allocation 前；rmdir 使用同一 checked decrement，零 count corruption 返回 `EIO` 而不 wrap。 | `kernel/src/syscall/fs/namespace.rs`, `kernel/src/syscall/fs/links.rs`, `kernel/src/fs/vfs/mutation.rs`, `kernel/src/fs/ext2/link_count.rs` |
| 43/44 | `statfs` / `fstatfs` | pathname/fd、RV64 120-byte `struct statfs` | 0；`EFAULT/EBADF` 及 pathname/filesystem errno | Linux filesystem statistics；musl statvfs；BusyBox df | **Complete**（当前 VFS/OFD kinds）。VFS 唯一解析 mount→filesystem；ext2 返回扣除 metadata overhead 的总块、reserved-adjusted available、superblock inode 计数与 UUID fsid，procfs/devfs/devpts/pipefs 返回 Linux simple-statfs 形状；character fd 保留 backing opened entry。 | `kernel/src/syscall/fs/statistics.rs`, `kernel/src/fs/vfs.rs`, `kernel/src/fs/ext2/filesystem.rs` |
| 46 | `ftruncate` | fd、64-bit length | 0；`EBADF/EFBIG/EISDIR/ENOSPC/EIO`；越过 limit 投递 SIGXFSZ | POSIX ftruncate | **Complete**。支持 `RLIMIT_FSIZE`、稀疏扩展、尾块清零、direct/三级 indirect 回收并维护 `i_blocks`。 | `kernel/src/syscall/fs.rs` |
| 47 | `fallocate` | fd、mode、signed offset/length | 0；`EBADF/EINVAL/EFBIG/ENOSPC/EIO/EOPNOTSUPP`；越过 limit 投递 SIGXFSZ | Linux space reservation；apk-tools extraction | **Partial（mode=0 complete）**。page-cache operation lock 与 ext2 mutation owner 串行化；只为 range 中的 hole 分配清零 block，不覆盖已有内容，必要时扩展 i_size。大 range 分成有界 JBD2 transaction，已提交 chunk crash-consistent；其他 allocation mode 明确返回 `EOPNOTSUPP`。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/page_cache.rs`, `kernel/src/fs/ext2.rs` |
| 48 | `faccessat` | dirfd、pathname、`F_OK/R_OK/W_OK/X_OK` | 0；`EACCES/EROFS/EINVAL` 与 pathname errno | musl access；BusyBox rm/command lookup | **Complete**。使用 real UID/GID 与 supplementary groups；VFS traversal 和 owner/group/other rwx、root execute、只读 `W_OK` 均成立。 | `kernel/src/syscall/fs/access.rs`, `kernel/src/fs/permission.rs` |
| 52-55 | `fchmod/fchmodat/fchownat/fchown` | fd 或 dirfd/pathname、mode/UID/GID/flags | 0；标准 fd/permission/path errno | POSIX chmod/chown；BusyBox、apk-tools | **Complete**（当前 inode-backed fd 与 ext2/devfs/procfs）。fd/path chmod/chown 共用唯一 owner/root 与 supplementary-group policy；chown 分别授权非 sentinel UID/GID，允许 unchanged live GID，并对每次 non-directory chown 按 Linux 规则清除 SUID、条件清除 SGID；sentinel-only 请求只有在实际 set-ID 清理物化为 mode change 时要求 owner/root。immutable caller identity 在 ext2 mutation lock 内对 live inode owner/mode 授权，拒绝不建立 rollback snapshot/journal，成功保持同锁直到 metadata commit，封闭 concurrent chown/chmod TOCTOU。symlink no-follow/empty-path chown 保持同一路径。 | `kernel/src/syscall/fs/attributes.rs`, `kernel/src/fs/permission.rs`, `kernel/src/fs/ext2/metadata.rs` |
| 49/50 | `chdir/fchdir` | NUL 结尾 raw pathname；directory fd | 0；`EBADF/ENOTDIR` 或 pathname/permission errno | POSIX cwd；musl/apk-tools | **Complete**（当前单 mount namespace）。pathname traversal 与 pathname/fd 最终目录都执行 search permission，成功后只替换 Process 唯一 opened cwd identity；rename 后的 `getcwd` 继续由同一 namespace owner 投影。 | `kernel/src/syscall/fs/open.rs`, `kernel/src/fs/vfs.rs` |
| 56/57 | `openat` / `close` | dirfd、raw path、flags/mode；fd | fd/0；标准 fd/path/permission errno | POSIX openat/close；Unix98 PTY；devkmsg | **Partial**。既有 OFD/device/flag 语义加 VFS traversal、final rwx、create parent、umask 与标准 opened-entry identity；cwd/directory fd/procfs magic link 共用该 seam，close 在 files lock 内原子移除 proc fd 节点，锁外完成 descriptor/OFD consequence。`/dev/ptmx` 每次 open 分配锁定 pair；独立 devpts 动态发布 `/dev/pts/N`，locked/master-closed slave open 返回 `EIO`，master last-close 原子移除节点并执行 vhangup consequence。devfs 另发布只读 `/dev/kmsg` 1:11，每个 OFD 独占 sequence cursor。尚无 `O_NOFOLLOW/O_PATH`。 | `kernel/src/syscall/fs/open.rs`, `kernel/src/fs/file/{character,descriptor_table}.rs`, `kernel/src/fs/{pty,devpts}.rs`, `kernel/src/fs/vfs/opened.rs` |
| 59 | `pipe2` | `int[2]`、`O_CLOEXEC/O_NONBLOCK` | 0；`EFAULT/EINVAL/EMFILE/ENOMEM` | POSIX pipe；BusyBox pipeline | **Complete**（当前 fd/wait 模型）。64 KiB data ring 以最多两段连续 copy 完成 wrap-around、4096-byte PIPE_BUF atomic write、blocking/nonblocking、EOF、EPIPE+SIGPIPE、fork/dup endpoint lifecycle 与 exit-time fd close 共用单一 Pipe owner。 | `kernel/src/ipc.rs`, `kernel/src/syscall/fs.rs` |
| 61 | `getdents64` | fd、`linux_dirent64` buffer、length | bytes；`EBADF/ENOTDIR/EFAULT/EINVAL` | libc readdir backend | **Complete**。目录 OFD 保存逐项位置，记录按 8 bytes 对齐并包含 `d_type`；并发目录变更后的 cookie 与 Linux 一样不承诺快照语义。 | `kernel/src/syscall/fs.rs` |
| 62 | `lseek` | fd、signed offset、whence | new offset；`EBADF/EINVAL/ESPIPE` | POSIX lseek | **Complete**。实现 `SEEK_SET/CUR/END`，offset 位于共享 OFD。 | `kernel/src/syscall/fs.rs` |
| 63-66 | `read/write/readv/writev` | fd、buffer 或 RV64 iovec | byte count；partial/EOF；标准 fd/stream errno | POSIX I/O；musl/BusyBox；DRM/evdev/PTY/devkmsg | **Partial（除 devkmsg blocking 外的当前 OFD kinds complete）**。scalar/vector 共用唯一 sequential descriptor dispatch，scalar 以单元素 vector 进入；regular 路径每次 syscall 只解析一次 cache facade并整次唯一持有 OFD offset，write/append 另以 per-inode write-sequence gate 防止跨 OFD chunk 穿插。所有 vector 路径一次导入 iovec，non-regular sequential 路径由唯一 cursor 维护 scatter/gather progress。socket datagram scalar/vector write 均提交一个完整消息，不按 staging 大小截断；stream 返回真实 short/partial count。Terminal noncanonical read 完整支持四种 VMIN/VTIME 组合、O_NONBLOCK、partial signal completion 与 inter-byte deadline。PTY master raw stream 与 slave line discipline 使用独立 byte/readiness channels；slave close 后 master read/write 返回 `EIO`，master close 后 slave read EOF、write `EIO`，已有输出先于 hangup error 读出。DRM read/readv 只返回完整 32-byte events，evdev 只返回整数个 RV64 24-byte `input_event`。`/dev/kmsg` 每次返回一个完整 `priority,sequence,timestamp,flags;message` record，小 buffer/覆盖/空返回 `EINVAL/EPIPE/EAGAIN`，当前不阻塞；DRM/evdev/kmsg write/writev 明确返回 EOPNOTSUPP。`RLIMIT_FSIZE` 越界首写返回 `EFBIG`+SIGXFSZ，跨界返回 partial；iovec 最大 1024、总长不超过 SSIZE_MAX。 | `kernel/src/syscall/fs/io/sequential.rs`, `kernel/src/syscall/fs/io/sequential/{read,write}.rs`, `kernel/src/syscall/fs/io/regular.rs`, `kernel/src/fs/{pty,file/{terminal,character}}.rs`, `kernel/src/{drm,input,log}.rs`, `kernel/src/fs/page_cache.rs` |
| 67-70 | `pread64/pwrite64/preadv/pwritev` | fd、buffer/iovec、signed 64-bit offset | byte count；partial/EOF；`EBADF/EFAULT/EFBIG/EINVAL/EIO/EISDIR/ESPIPE` | POSIX positioned I/O；SQLite/Git | **Complete**（当前 regular-file scope）。显式 offset 不改变共享 OFD offset；legacy pwrite 继承 Linux `O_APPEND` 行为。scalar/vector 共用一次 fd/cache 解析和同一 chunk/partial-I/O engine，write sequence 覆盖完整 iovec；stream、directory 与 anonymous fd 不伪造 positioned I/O。 | `kernel/src/syscall/fs/io/positioned.rs`, `kernel/src/syscall/fs/io/regular.rs` |
| 71 | `sendfile` | output fd、input fd、可空 signed 64-bit offset pointer、count | byte count/partial/EOF；`EBADF/EFAULT/EFBIG/EINVAL/ESPIPE` 与 storage errno | Linux in-kernel copy；Alpine CA bundle trigger | **Partial（regular→regular complete）**。输入/输出各解析一次 page-cache facade，以唯一 PAGE_SIZE kernel staging 避免 userspace round-trip；显式 offset 只在成功传输后推进用户值，空 offset 更新输入 OFD，输出 OFD 始终推进。两个 OFD offset 按 identity 排序后整次持有，输出 write-sequence 覆盖完整 operation；支持 MAX_RW_COUNT、RLIMIT_FSIZE/SIGXFSZ、partial result、同文件非重叠 copy，并拒绝 O_APPEND/重叠区间。socket/pipe 输出仍返回 `EINVAL`，不伪造 zero-copy。 | `kernel/src/syscall/fs/io/sendfile.rs`, `kernel/src/fs/page_cache.rs` |
| 72-73 | `pselect6` / `ppoll` | fd_set/pollfd array、relative timespec、可选临时 sigmask | ready count/0；`EINTR/EBADF/EFAULT/EINVAL/ENOMEM` | POSIX select/poll；musl；OpenSSL；BusyBox line editing；evdev/PTY | **Partial（除 devkmsg async wait 外的当前 OFD kinds complete）**。两种 ABI 共用一次 registration 可索引多个 Pipe/Console source；evdev 与 PTY 各自以独立 notification Pipe 唤醒并按 backend level state 复查，PTY peer close 无条件发布 POLLHUP。`/dev/kmsg` 只支持调用时的同步 readable level，空 reader 尚无 record-publication wait source，因此不声称阻塞 poll wake。pselect 按 read/write/except 输出 bitmap并拒绝 invalid fd，ppoll 对 invalid fd 返回 POLLNVAL；timeout 使用 monotonic deadline，临时 mask 在 ready/timeout 或 signal frame正确恢复。 | `kernel/src/syscall/poll.rs`, `kernel/src/task/task_manager.rs`, `kernel/src/{input,fs/pty,log}.rs` |
| 78 | `readlinkat` | dirfd、raw path、buffer、size | target byte count；不追加 NUL；标准 pathname errno | Linux readlinkat；BusyBox ls/readlink/tty | **Complete**（当前 VFS symlink 语义）。支持绝对路径、`AT_FDCWD`、目录 fd、截断复制、persistent raw target 与 `/proc/<pid>/fd` dynamic target；opened entry 被删除时返回 ` (deleted)`，magic-link follow 仍引用 live file。 | `kernel/src/syscall/fs/readlink.rs`, `kernel/src/fs/vfs.rs`, `kernel/src/fs/procfs.rs` |
| 79/80 | `newfstatat` / `fstat` | dirfd/fd、path、RV64 `struct stat` | 0；fd/path/copyout errno | POSIX stat/fstatat；BusyBox ls/lstat/tty | **Partial**。128-byte asm-generic 布局与 512-byte `st_blocks` 正确；`newfstatat` 默认跟随 symlink，`AT_SYMLINK_NOFOLLOW` 保留末项 link inode；pathname-backed character fd 的 `fstat` 使用打开时的真实 filesystem/inode identity，因此 `/proc/self/fd` target 与 descriptor identity 可供 `ttyname` 精确匹配。尚无 `AT_EMPTY_PATH`、credentials 与完整 mount semantics。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/vfs.rs` |
| 81 | `sync` | 无参数 | 按 Linux ABI 固定返回 0，单个 writeback error 不从该入口报告 | POSIX sync；BusyBox sync applet | **Complete**（当前单 root filesystem）。经 VFS 唯一 root seam 等待已提交写并在 VirtIO 声明 FLUSH feature 时发送 device flush；后续冷启动读回验证持久化结果。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/vfs.rs` |
| 82 | `fsync` | fd | 0；`EBADF/EINVAL/EIO` | POSIX fsync | **Complete**。inode-backed fd 等待所有同步写完成，并在 VirtIO 声明 FLUSH feature 时发送 flush request；pipe/character fd 返回 `EINVAL`。 | `kernel/src/syscall/fs.rs` |
| 83 | `fdatasync` | fd | 0；`EBADF/EINVAL/EIO` | POSIX fdatasync；SQLite/Git | **Complete**（当前 ext2 mutation model）。当前 metadata journal/checkpoint 无可安全省略的独立 data-only barrier，因此与 `fsync` 共用 inode writeback + VirtIO FLUSH seam，语义更强但不另建 durability 路径。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/vfs.rs` |
| 88 | `utimensat` | dirfd、pathname、可选两元素 RV64 timespec、`AT_SYMLINK_NOFOLLOW` | 0；标准 time/path/permission errno | POSIX futimens/utimensat；BusyBox touch | **Partial**。支持 owner/root arbitrary-time 与 writable inode NOW policy，并 journal 持久化 atime/mtime/ctime；仍无 `AT_EMPTY_PATH` 与纳秒磁盘字段。 | `kernel/src/syscall/fs.rs`, `kernel/src/fs/ext2/metadata.rs` |
| 276 | `renameat2` | old/new dirfd/path、flags | 0；标准 rename errno；正 parent-link delta 达 ext2 上限返回 `EMLINK` | Linux renameat2 | **Partial**。支持跨目录移动、journal-atomic 替换、`RENAME_NOREPLACE`、中间 symlink traversal 与最终目录项不跟随。目录 parent 的 same/cross-parent net delta 在首个 namespace edit 前 checked planning：饱和 destination 的新增移动返回 `EMLINK`，替换目录因 net delta 为零仍成功；每个 parent 在既有四-inode transaction 中只发布一次最终 count。无其他 rename flags、credentials 与 mount semantics。 | `kernel/src/syscall/fs/namespace.rs`, `kernel/src/fs/ext2/{directory,link_count}.rs` |
| 93 | `exit` | `int status` | 不返回 | POSIX `_exit`；musl `_Exit` fallback | **Complete**。只终止 calling Thread；最后一个 Thread 才发布 Process zombie，并原子完成 reparent、controlling-session SIGHUP/release，以及 newly orphaned stopped group 的 SIGHUP→SIGCONT。 | `kernel/src/syscall/process.rs`, `kernel/src/task/task_manager/process_exit.rs`, `kernel/src/task/task_manager/process_group.rs` |
| 94 | `exit_group` | `int status` | 不返回 | Linux extension；musl `_Exit` 首选 | **Complete**。首个 caller 唯一提交低 8-bit group status，解除全部 sibling wait 并请求远端 CPU 调度；每个 Thread 在自己的内核栈执行 robust/clear-tid/调度清理，最后一个 Thread 经同一 Process exit lifecycle 发布 zombie。 | `kernel/src/syscall/process.rs`, `kernel/src/task/task_manager/process_exit.rs` |
| 96 | `set_tid_address` | clear-child-tid pointer | calling TID | Linux thread runtime；musl pthread | **Complete**。零清除 registration；thread exit 写零并 futex-wake 一个 waiter。 | `kernel/src/syscall/process.rs` |
| 98 | `futex` | uaddr、op、val、timeout/val2、uaddr2、val3 | 0 或 wake/requeue count；`EAGAIN/EFAULT/EINTR/EINVAL/ETIMEDOUT/ENOSYS` | Linux thread/process synchronization；musl pthread | **Partial（完整 non-PI scope）**。实现 WAIT/WAKE、REQUEUE/CMP_REQUEUE、WAIT/WAKE_BITSET、PRIVATE flag、WAIT relative monotonic timeout与 WAIT_BITSET absolute monotonic/realtime timeout。private key 使用 AddressSpace identity，anonymous/file shared key 使用 backing/file identity + offset；统一 registration 的 bitset、deadline 与 wait ID 在 requeue 后保持不变，wake/timeout/signal 只能消费一次。无 timeout WAIT 在 `SA_RESTART` 下重放；带 timeout wait 保持 `EINTR`。尚无 PI/PI-requeue、WAKE_OP。 | `kernel/src/syscall/futex.rs`, `kernel/src/task/task_manager/futex.rs`, `kernel/src/task/task_manager/wait_registry.rs`, `kernel/src/memory/mm/futex_key.rs` |
| 99 | `set_robust_list` | head、len | 0；`EINVAL` | Linux robust mutex；musl pthread | **Complete**（当前 non-PI Thread 模型）。RV64 24-byte head，NULL 注销；全局 TID 保持在 30-bit owner mask 内且 namespace 耗尽的 clone 返回 `EAGAIN`；exit 与 exec old-mm release 均有界清理，OWNER_DIED CAS conflict 用返回值重查 owner 后重试，成功且旧值含 WAITERS 才 wake；`list_op_pending` zero-owner 窗口补 wake且不重复处理已入链节点，非法用户链停止清理。 | `kernel/src/syscall/process.rs`, `kernel/src/task/model/robust_list.rs` |
| 101/115 | `nanosleep/clock_nanosleep` | relative timespec，或 clock ID、flags、timespec；可选 remaining | `0`；`EFAULT/EINVAL/EINTR/EOPNOTSUPP` | POSIX sleep；musl cancellation-point wrapper | **Partial（wall-clock scope）**。两者共用 absolute monotonic wait seam；clock_nanosleep 支持 `CLOCK_REALTIME/MONOTONIC` 的 relative 与 `TIMER_ABSTIME`，past deadline 立即成功，relative 被 signal 中断才 copyout remaining，absolute 不修改。Linux common-clock 路径只解释 `TIMER_ABSTIME` 位；其他 flags 不另造语义。即使 handler 含 `SA_RESTART` 也不自动重启；无法用 `u64` 纳秒表达的请求返回 `EINVAL`。Process/Thread CPU-clock sleep 尚无 CPU timer owner，明确返回 `EOPNOTSUPP`。 | `kernel/src/syscall/timer.rs`, `kernel/src/task/task_manager.rs` |
| 102/103 | `getitimer/setitimer` | `ITIMER_REAL`、RV64 32-byte `itimerval` | 0；`EFAULT/EINVAL` | musl `alarm/setitimer`；BusyBox nc | **Partial**。独立 RealTimerQueue 唯一拥有 fork 不继承、exec 保留、exit 清理的 realtime timer record 与 active deadline index；timer deferred 按 deadline 有序消费并发布 coalesced SIGALRM，支持 one-shot/periodic、稳定周期相位与旧值/剩余值。`ITIMER_VIRTUAL/PROF` 尚未开放。 | `kernel/src/syscall/timer.rs`, `kernel/src/task/task_manager/deferred.rs` |
| 113/114 | `clock_gettime/clock_getres` | `clockid_t, struct timespec *`；RV64 `i64,i64`；getres 输出可为空 | `0`；`EINVAL/EFAULT` | POSIX `clock_gettime/clock_getres/clock()`；musl direct wrapper/vDSO fallback | **Partial**。支持 `CLOCK_REALTIME(0)`、`CLOCK_MONOTONIC(1)`、`CLOCK_PROCESS_CPUTIME_ID(2)` 与 `CLOCK_THREAD_CPUTIME_ID(3)`。CPU clocks 从 scheduler 唯一累计值只读投影 calling Thread 的 active slice；Process clock 保留已退出 sibling 的累计时间，其他 hart 上尚未 tick/deschedule 的 sibling 与 Linux 一样允许短暂滞后。getres 对 wall clocks 返回 DTB timebase 换算后的向上取整纳秒粒度，对当前微秒记账的 CPU clocks 返回 1000ns，空输出只校验 selector。其他 Linux clock ID 返回 `EINVAL`。 | `kernel/src/syscall/timer.rs`, `kernel/src/task/model/scheduling.rs`, `kernel/src/timer.rs` |
| 118-121 | `sched_setparam/sched_setscheduler/sched_getscheduler/sched_getparam` | signed TID、4-byte `struct sched_param`；setscheduler 另含 policy | 0 或 policy；`EINVAL/EFAULT/ESRCH/EPERM` | POSIX scheduler API；musl direct wrappers | **Partial（完整 `SCHED_OTHER` scope）**。零选择 caller，正数按全局 live TID 查询；legacy priority 必须为 0。`Sched` policy lock 唯一拥有 `SCHED_RESET_ON_FORK`，setparam 保留该位，setscheduler 可设置；普通 owner 不得清除，fork/clone 消费后将 child 的负 nice 归零并清位，exec 保留。当前无 FIFO/RR/BATCH/IDLE/DEADLINE/EXT scheduling class，setter 对其返回 `EINVAL`。 | `kernel/src/syscall/scheduler.rs`, `kernel/src/task/task_manager/policy.rs`, `kernel/src/task/model/scheduling.rs` |
| 122/123 | `sched_setaffinity/sched_getaffinity` | signed TID selector、变长 logical CPU bitmap；get 长度至少 8 且按 8 对齐 | set 返回 0，get 返回 8；`EINVAL/EFAULT/ESRCH/EPERM` | Linux scheduler affinity；musl `sched_*affinity/pthread_*affinity_np/sysconf` | **Complete**（当前 immutable topology/credentials model）。零选择 caller，正数按 TaskManager 全局 live TID 查询；set 先按 Linux 规则 copy 最多 8 bytes（短 mask 高位清零、长 mask 尾部忽略），再执行 effective-UID/root permission 并与 active topology 相交。SchedulingState 单锁拥有 mask；fork/clone 继承、exec 保留，所有 placement/wake/preempt/continue 选核均过滤 affinity。Ready entry 以 generation 换代，Running/切出 ownership 在 syscall 成功前同步离开禁止 CPU。get 返回 owner mask 与 active CPUs 的交集，musl 清零更长缓冲尾部。当前无 CPU hotplug、cpuset/cgroup 或 user namespace。 | `kernel/src/syscall/scheduler.rs`, `kernel/src/task/task_manager/affinity.rs`, `kernel/src/task/processor/placement.rs`, `kernel/src/task/model/scheduling.rs` |
| 124 | `sched_yield` | 无参数 | `0` | POSIX `sched_yield`；musl direct wrapper | **Complete**。当前 task 回到唯一 CFS-like runqueue。 | `kernel/src/syscall/scheduler.rs` |
| 125/126 | `sched_get_priority_max/sched_get_priority_min` | Linux scheduler policy | priority bound；`EINVAL` | POSIX scheduler API；musl direct wrappers | **Complete**。严格按 Linux v7.1 policy table返回 FIFO/RR 的 99/1 与 OTHER/BATCH/IDLE/DEADLINE/EXT 的 0/0；未知 policy 与 flag 返回 `EINVAL`。bounds 查询不宣称对应 scheduling class 已实现。 | `kernel/src/syscall/scheduler.rs` |
| 127 | `sched_rr_get_interval` | signed TID、RV64 16-byte `__kernel_timespec` | 0；`EINVAL/EFAULT/ESRCH` | POSIX scheduler API；musl direct wrapper | **Partial（固定 `SCHED_OTHER` quantum）**。按 Linux 顺序先拒绝负 TID、再选择 caller/全局 live TID、最后 copyout。返回 timer owner 从实际 tick interval 与 DTB timebase 换算的基础抢占时间片；当前没有 Linux EEVDF per-entity 动态 slice、空闲 runqueue 的零 interval，也没有 RT RR class。 | `kernel/src/syscall/scheduler.rs`, `kernel/src/task/task_manager/policy.rs`, `kernel/src/timer.rs` |
| 129 | `kill` | signed pid selector、signal | 0；`ESRCH/EINVAL/EPERM` | POSIX kill；BusyBox kill/job control | **Complete**（当前 standard-signal model）。既有 selector/job-control 语义加 real/effective→target real/saved UID permission、root 与 SIGCONT same-session exception；zero probe 同样校验权限。multi-process selector 对每个 candidate 在一次 process-graph transaction 内完成 live selection、permission 与 probe/generation，成功只在 permitted probe 或 generation attempt 完成后计数，且任一成功不会被后续 denial/exit 覆盖；notification/wakeup 在 graph lock 外执行。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/task_manager/signal.rs` |
| 130 | `tkill` | tid、signal | 0；`ESRCH/EINVAL/EPERM` | Linux legacy thread selector；musl/BusyBox raise path | **Complete**（当前 Thread model）。全局 TID、SI_TKILL 与统一 credentials permission seam。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/task_manager/signal.rs` |
| 131 | `tgkill` | tgid、tid、signal | 0；`ESRCH/EINVAL/EPERM` | Linux thread-directed signal；musl pthread | **Complete**（当前 Thread model）。TGID/TID existence、SI_TKILL、wait interruption与统一 credentials permission seam。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/task_manager/signal.rs` |
| 132 | `sigaltstack` | 可选 RV64 24-byte `stack_t` new/old | 0；`EFAULT/EINVAL/ENOMEM/EPERM` | POSIX alternate signal stack；musl direct wrapper | **Complete**（当前 fixed RV64 signal-frame scope）。Thread 唯一拥有 registration，`SS_ONSTACK` 从当前 SP/range 动态投影；支持 disable、2048-byte minimum、`SS_AUTODISARM`、`SA_ONSTACK`、nested overflow、frame `uc_stack` 保存/恢复，以及 fork/vfork 继承、普通 `CLONE_VM` Thread 与 exec 清空。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/model/alternate_signal_stack.rs`, `kernel/src/task/model/signal_state.rs` |
| 133 | `rt_sigsuspend` | 8-byte sigset | 固定 `EINTR` | POSIX sigsuspend；BusyBox wait | **Complete**（当前 signal set）。临时 mask 与 Signal membership 原子封闭 lost wakeup；pending bit 不被 waiter 消费，trap frame 恢复调用前 mask。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/model.rs` |
| 134/135 | `rt_sigaction` / `rt_sigprocmask` | RV64 24-byte action、8-byte sigset | 0；`EFAULT/EINVAL` | POSIX signal API；musl wrappers | **Partial**。Process disposition、per-Thread mask、SIGKILL/SIGSTOP 不可屏蔽；`SA_ONSTACK` 消费 Thread altstack seam，handler 的 `SA_RESTART` 可重放 blocking `wait4` 与无 timeout 的 futex WAIT；default stop action 在 SIGCONT 后无条件恢复被中断 ecall。无 queued realtime value 与其他 syscall 的完整 restart coverage。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/model/signal_state.rs` |
| 137 | `rt_sigtimedwait` | 8-byte sigset、可选 128-byte siginfo、可选 relative timespec | signal number；`EAGAIN/EINTR/EFAULT/EINVAL` | POSIX sigwaitinfo/sigtimedwait；BusyBox init | **Partial**。依次消费 Thread pending 与 Process shared pending 中的 coalesced standard signal，支持无限等待、零/有限 monotonic timeout，以及 `SI_TKILL`、`SI_USER`、`SIGCHLD/CLD_EXITED/CLD_KILLED` siginfo；registration 共用 indexed wait/deadline registry。无 realtime queued value。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/task_manager/signal.rs` |
| 139 | `rt_sigreturn` | frame 隐式位于 sp | 恢复 a0/context；坏 frame 最终终止 | Linux RV64 signal ABI | **Partial**。恢复 32 GPR、32 FP、fcsr、PC 与 mask；frame 使用固定 Linux v7.1 RV64 layout和 U|RX return trampoline。无 vector/CFI extension context。 | `kernel/src/syscall/signal.rs`, `kernel/src/task/model.rs` |
| 140/141 | `setpriority/getpriority` | `PRIO_PROCESS/PRIO_PGRP/PRIO_USER`、TID/PGID/UID、nice | set 返回 0；raw get 返回 1..40；`EACCES/EINVAL/EPERM/ESRCH` | POSIX get/setpriority/nice；musl direct wrappers | **Complete**（当前 single-user-namespace model）。PROCESS 零选择 caller、非零按全局 TID；PGRP/USER 覆盖全部 live Thread。get 返回集合中最小 nice 的 `20-nice`；set 钳制 -20..19，并保持 Linux 多目标部分成功及后续错误优先级。effective UID 必须匹配 target real/effective UID或为 root；提高优先级还受 target `RLIMIT_NICE` soft 限制。Running target 请求 reschedule，active slice 冻结 dispatch 时权重，避免远端修改追溯计费。 | `kernel/src/syscall/scheduler.rs`, `kernel/src/task/task_manager/policy.rs`, `kernel/src/task/model/scheduling.rs` |
| 142 | `reboot` | magic1、magic2、command、argument | CAD command 返回 0；reset 成功不返回；`EINVAL/EIO` | BusyBox init shutdown policy | **Partial**。校验 Linux magic，`CAD_OFF/CAD_ON` 更新唯一 system policy，`RESTART/HALT/POWER_OFF` 映射 SBI SRST cold reboot/shutdown。platform 无 restart-reason channel，因此拒绝 `RESTART2`；无 kexec/suspend。 | `kernel/src/syscall/reboot.rs`, `kernel/src/system.rs` |
| 144/146-150/158-159/166/174-177 | credentials 与 umask | Linux uid/gid/group/mask ABI | ID/0/old mask；`EFAULT/EINVAL/ENOMEM/EPERM` | POSIX/musl；BusyBox id | **Complete**（单 user namespace、无 capabilities）。Process 单锁拥有 real/effective/saved IDs、supplementary groups 与 umask；Thread 共享、fork 复制、exec set-id 原子提交。 | `kernel/src/syscall/credentials.rs`, `kernel/src/task/model/credentials.rs` |
| 154-157 | `setpgid/getpgid/getsid/setsid` | PID/PGID 或无参数 | 0/ID；`ESRCH/EPERM/EACCES` | POSIX session/job control；BusyBox init/ash | **Partial**。process graph 唯一拥有 SID/PGID/exec generation 与既有 job-control 生命周期；无 PID namespace。 | `kernel/src/syscall/process.rs`, `kernel/src/task/task_manager/process_group.rs` |
| 160 | `uname` | RV64 390-byte `struct new_utsname *` | 0；`EFAULT` | POSIX uname；musl direct wrapper；BusyBox uname/arch | **Complete**（当前 immutable identity 模型）。system module 唯一提供 `LiteOS/liteos/<package-version>/#1 SMP PREEMPT/riscv64/(none)` 六字段；每字段 65 bytes 且尾部清零。无 sethostname/setdomainname 或 UTS namespace。 | `kernel/src/syscall/system_identity.rs`, `kernel/src/system.rs` |
| 168 | `getcpu` | 可选 `unsigned *cpu/node`、忽略 cache pointer | 0；`EFAULT` | Linux CPU locality；musl `sched_getcpu` fallback | **Complete**（当前单 NUMA domain）。system façade 将有序 DTB hart table 紧凑映射为 Linux logical CPU index；CPU 与 node copyout 依 Linux 顺序分别尝试，node 固定为 0。raw hart ID 不泄漏到该 ABI；同一映射也用于 procfs processor 字段与 hwprobe CPU mask。 | `kernel/src/syscall/system_info.rs`, `kernel/src/system.rs`, `kernel/src/arch/riscv64/hart.rs` |
| 169 | `gettimeofday` | 可选 RV64 16-byte timeval、可选 8-byte timezone | 0；`EFAULT` | Linux legacy time API；musl compatibility | **Complete**（当前 realtime 模型）。与 `clock_gettime(CLOCK_REALTIME)` 共用 RTC+monotonic offset，microseconds 向下截断；按 Linux 顺序先 copy timeval 再 copy timezone，固定 UTC policy 返回 `{0,0}`。无 settimeofday/timezone mutation。 | `kernel/src/syscall/timer.rs`, `kernel/src/timer.rs` |
| 172 | `getpid` | 无参数 | TGID | POSIX `getpid`；musl direct wrapper | **Complete**。返回 Process owner 的 TGID，不从 scheduler ID 推导。 | `kernel/src/syscall/process.rs` |
| 173 | `getppid` | 无参数 | parent TGID；init 为 0 | POSIX `getppid`；musl direct wrapper | **Complete**。读取 TaskManager 唯一 parent edge；orphan 重新指向 PID 1。 | `kernel/src/syscall/process.rs` |
| 174-177 | `getuid/geteuid/getgid/getegid` | 无参数 | 当前 Process ID | POSIX identity；BusyBox id | **Complete**。读取 Process 唯一 credentials owner 的 real/effective UID/GID。 | `kernel/src/syscall/credentials.rs` |
| 178 | `gettid` | 无参数 | TID | Linux extension；musl pthread internals | **Complete**。单线程模型中 TID == TGID，但值来自 ThreadContext owner。 | `kernel/src/syscall/process.rs` |
| 179 | `sysinfo` | `struct sysinfo *`；RV64 112-byte UAPI layout | 0；`EFAULT` | Linux system information；musl `sysinfo`；BusyBox free/uptime | **Complete**（当前资源模型）。uptime 按 Linux 向上取整到秒；load 使用 Linux FSHIFT=11、5-second 1/5/15-minute EWMA 与 missed-period `calc_load_n` 后在 ABI seam 转为 `SI_LOAD_SHIFT=16`，当前 active scope 为 runnable Thread（尚无 uninterruptible task state）；`procs` 为 live thread 数，RAM 字段以 byte 值配合 `mem_unit=1`。当前无 swap/highmem，相关字段为零；page-cache 统计只由 procfs meminfo 消费，不改变 sysinfo UAPI 字段。 | `kernel/src/syscall/system_info.rs`, `kernel/src/task/task_manager/{load_average,procfs}.rs` |
| 198-212, 242 | AF_UNIX、AF_INET UDP/TCP/raw ICMP、AF_PACKET 与 AF_NETLINK socket | socket/sockaddr/iovec/msghdr/cmsg/options | fd/count；标准 socket errno | Linux sockets；musl；BusyBox nc/udhcpc/ping/wget；OpenSSL；DRM hotplug client | **Partial（明确 domain/protocol scope）**。完整 message/backpressure/poll/option 与未开放边界见 [Socket ABI contract](syscall/socket.md)。本阶段新增 `NETLINK_KOBJECT_UEVENT` group 1 的只读 DRM hotplug multicast；AF_NETLINK send/connect/peer/MSG_PEEK、其他 protocol/group、rtnetlink 与 userspace publish 仍未开放。 | `kernel/src/syscall/user_iovec.rs`, `kernel/src/syscall/socket/{message,options}.rs`, `kernel/src/socket/{message_limits,packet,inet,unix,kobject,observation}.rs`, `kernel/src/socket/inet/{raw,options,udp,tcp,wait}.rs` |
| 214 | `brk` | `unsigned long new_brk` | 成功返回新 break；失败返回未改变旧 break，无负 errno | Linux legacy VM；musl compatibility path | **Complete**。精确 byte break 由 `MemorySet` 单独拥有，实际页复用可拆分的 anonymous lazy VMA；`MAP_FIXED` 可替换 break 范围内的页，`RLIMIT_DATA/AS`、冲突或 OOM 均保持旧 break。 | `kernel/src/syscall/memory.rs` |
| 215 | `munmap` | page-aligned address、nonzero length | `0`；`EINVAL/EACCES/EIO` | POSIX `munmap`；musl allocator/loader；DRM dumb mapping | **Complete（当前 VMA 类型）**。支持 anonymous、file private/shared 与 device-backed VMA 删除、洞忽略和左右拆分；file shared dirty range 先同步写回，device VMA 只撤销 PTE/Arc view，系统 VMA 返回 `EACCES`。 | `kernel/src/syscall/memory.rs`, `kernel/src/memory/mm/mmap.rs` |
| 220 | `clone` | flags、stack、parent_tid、tls、child_tid | parent=child PID/TID、child=0；标准 errno | Linux process/thread primitive；musl fork/vfork/pthread；BusyBox spawn | **Partial**。支持 fork-shaped COW process clone；`CLONE_VM|CLONE_VFORK|SIGCHLD` child 使用独立 Process handle、精确共享同一 AddressSpace，并只挂起发起调用的 parent Thread 到 child exec detach/exit；parent sibling 可继续运行并并发 spawn。支持 VM/FS/FILES/SIGHAND/THREAD/SYSVSEM/SETTLS 配合 parent/child-set/clear-tid 的 thread clone，并按 Linux 语义忽略历史 `CLONE_DETACHED`。普通多线程 fork 仍返回 `EAGAIN`，无 namespace/pidfd flags。 | `kernel/src/syscall/process.rs`, `kernel/src/task/model/process_clone.rs`, `kernel/src/task/task_manager/vfork.rs` |
| 221 | `execve` | `const char *path, char *const argv[], char *const envp[]`；raw NUL bytes | 成功不回旧映像；标准 errno | POSIX `execve`；musl direct wrapper | **Partial**。既有 ELF/script/dynamic transaction 加 credential-aware search/execute 与最终 ELF setuid/setgid；set-id 仅在映像成功准备后的不可失败 commit 生效。CLOEXEC cleanup 以 transient cursor 和固定 stack batch 单次扫描 FDSize，锁外关闭 entry，不随 CLOEXEC 数量重复从 slot zero 搜索。多线程 Process 仍返回 `EAGAIN`。 | `kernel/src/syscall/process.rs`, `kernel/src/task/loader.rs`, `kernel/src/task/model/{process_exec,file_descriptions}.rs`, `kernel/src/fs/file/descriptor_table.rs` |
| 222 | `mmap` | address、length、prot、flags、fd、offset | address；`EINVAL/EACCES/EEXIST/ENOMEM/EIO/EOVERFLOW` | POSIX `mmap`；musl allocator/loader；V8 reservation/JIT；DRM dumb mapping | **Complete（anonymous + regular-file + current DRM device scope）**。既有 anonymous/file lazy residency、COW/coherence、EOF、resource-limit 与 fixed semantics 保持不变；regular-file offset 与 rounded length 在移除 overlap 前按 RV64 Linux `MAX_LFS_FILESIZE` 固化为 validated page interval，alignment/zero length 返回 `EINVAL`，signed file range overflow 返回 `EOVERFLOW`，split/fault/writeback/futex 不再重算 raw byte offset。fault 先完成 membership、permission 与 private EOF 分类，预检已拒绝的路径不分配或回收 frame；page-cache fault lookup 与 truncate 共用 operation domain，在 cache node/shared frame/private frame 分配前判定稳定 EOF，并以返回 page pin 线性化并发 truncate，越过 EOF 直接交付 `SIGBUS`。`MAP_FIXED` 在移除 overlap 前完成 anonymous fd/offset 与 file/device fd、access、type、permission、backing 校验，后续 VMA installation failure 不承诺恢复旧 mapping。DRM 只接受 MAP_SHARED、MAP_DUMB 返回的 exact file-private offset、object size 内长度及 non-executable permission；VMA 直接持 backing Arc，fork/split 共享，handle destroy/close 不破坏 live mapping。当前无其他 device mmap、tmpfs/shmem pathname object 或 swap/commit reservation owner。 | `kernel/src/syscall/memory.rs`, `kernel/src/memory/mm/file_page_range.rs`, `kernel/src/memory/mm/mmap/`, `kernel/src/fs/page_cache.rs`, `kernel/src/drm.rs` |
| 226 | `mprotect` | page-aligned address、length、prot | `0`；`EINVAL/EACCES` | POSIX `mprotect`；musl loader；V8 JIT；DRM mapping | **Complete（当前 VMA 类型）**。anonymous/file/ELF private、anonymous/file shared 与 device VMA 可拆分并切换权限；device mapping 允许 NONE/READ/WRITE、拒绝 executable permission并保持同一 backing identity。 | `kernel/src/syscall/memory.rs`, `kernel/src/memory/mm/mmap.rs` |
| 227 | `msync` | page-aligned address、length、`MS_*` | `0`；`EINVAL/ENOMEM/EIO` | POSIX `msync`；musl | **Complete（当前 VMA 类型）**。固定 Linux flag validation；MS_ASYNC 只校验 coverage、不启动 I/O，MS_SYNC 对 file MAP_SHARED 区间同步 page cache、ext2 journal 与 block flush；hole 最终 ENOMEM。 | `kernel/src/syscall/memory.rs`, `kernel/src/memory/mm/mmap.rs`, `kernel/src/fs/page_cache.rs` |
| 233 | `madvise` | page-aligned address、length、advice | `0`；`EINVAL/ENOMEM/EIO` | Linux VM policy；musl allocator | **Partial（common residency scope）**。实现 NORMAL/RANDOM/SEQUENTIAL、WILLNEED、DONTNEED、FREE；DONTNEED 丢弃 private/page-cache residency并保持对应 backing 语义，FREE 只接受 private anonymous/stack。不可 page-in/reclaim 的 DRM device VMA 明确拒绝 DONTNEED，避免保留 VMA 却制造永久缺页。未开放 COLD/PAGEOUT/HUGEPAGE/mergeable/fork-dump policy。 | `kernel/src/syscall/memory.rs`, `kernel/src/memory/mm/mmap/advice.rs` |
| 278 | `getrandom` | buffer、length、`GRND_NONBLOCK/GRND_RANDOM` | 字节数；`EFAULT/EINVAL/EIO` | Linux entropy API；musl | **Complete**（virtio-rng 基线）。唯一 entropy source 为 virtio-rng；设备失败不回退 RTC/timer。 | `kernel/src/syscall/random.rs`, `kernel/src/drivers/virtio_rng.rs` |
| 286/287 | `preadv2/pwritev2` | fd、iovec、split signed offset、RWF flags | byte count；partial/EOF；标准 positioned-I/O errno 与 `EOPNOTSUPP` | Linux vector I/O；SQLite | **Partial（同步 regular-file scope）**。offset=-1 复用共享 OFD offset；pwritev2 支持 `RWF_DSYNC/SYNC/APPEND/NOAPPEND`，同步 flags 经唯一 fd sync seam；preadv2 flags=0。`RWF_HIPRI/NOWAIT/ATOMIC/DONTCACHE` 无对应设施，明确返回 `EOPNOTSUPP`。 | `kernel/src/syscall/fs/io/positioned.rs` |
| 283 | `membarrier` | command、flags、cpu_id | command mask/0；`EINVAL/EPERM` | Linux memory-ordering ABI；musl pthread/dynamic TLS | **Partial（private expedited scope）**。实现并只由 QUERY 宣告 `REGISTER_PRIVATE_EXPEDITED` 与 `PRIVATE_EXPEDITED`；registration 属于 AddressSpace，CLONE_VM/vfork 共享而 fork/exec 重置。执行路径以 generation + SBI IPI 等待所有 active DTB hart 完成 full fence，语义强于 mm-target mask但不缩减 ordering。其他 global/sync-core/rseq/get-registrations command 返回 `EINVAL`，非零 flags 返回 `EINVAL`。 | `kernel/src/syscall/membarrier.rs`, `kernel/src/task/memory_barrier.rs`, `kernel/src/arch/riscv64/hart/memory_barrier.rs` |
| 260 | `wait4` | pid、status、options、rusage | child PID/0；标准 errno，含 `EINTR` | POSIX waitpid backend；musl direct wrapper | **Partial**。多线程 caller 支持 PID、任一 child、caller/explicit process group selector、blocking/`WNOHANG`、`WUNTRACED`、`WCONTINUED`、独立消费 exited/signaled/stopped/continued status、signal interruption与 copyout-before-consume；handler 含 `SA_RESTART` 时透明重放。process graph 的 per-TID waiter 与唯一 event claim 防止并发重复回收；copyout 失败释放 claim。rusage 必须为空。 | `kernel/src/syscall/process.rs`, `kernel/src/task/task_manager/wait_child.rs` |
| 261 | `prlimit64` | pid、resource、可选 16-byte new/old rlimit64 | `0`；`EFAULT/EINVAL/EPERM/ESRCH` | Linux resource control；musl get/setrlimit | **Partial（现有资源域完整）**。Process 单锁 owner，Thread 共享、fork 复制、exec 保留；跨进程 real-ID/root permission 与 soft/hard rules成立。实际约束 CPU/FSIZE/DATA/STACK/NPROC/NOFILE/AS/NICE；RSS 按 Linux advisory 忽略，CORE/MEMLOCK/LOCKS/SIGPENDING/MSGQUEUE/RTPRIO/RTTIME 因对应设施尚不存在只保存标准值。 | `kernel/src/syscall/resource_limit.rs`, `kernel/src/task/model/resource_limits.rs`, `kernel/src/task/task_manager/resource_limit.rs` |
| 258 | `riscv_hwprobe` | pair array、可选 CPU mask、flags | 0；`EFAULT/EINVAL` | Linux/riscv64 CPU capability discovery；OpenSSL | **Partial（value-query scope）**。flags=0 支持 pair copyin/out、unknown key→`{-1,0}`、logical CPU mask 与 all-online shortcut；system façade 保守投影 IMA/FD/C、Sv39 user limit、time CSR frequency 和 vector-misaligned unsupported，不宣告未经 DTB 证明的扩展。`RISCV_HWPROBE_WHICH_CPUS` 尚未开放。 | `kernel/src/syscall/riscv_hwprobe.rs`, `kernel/src/system.rs` |

文件 ABI 与原有 process/time/memory ABI 的精确边界均以本表为准；“Complete”只覆盖当前明确存在的对象类型与已声明并发模型。

## 3. 阶段记录边界

历史 Phase 表只表示当时状态；当前编号、实现范围与缺口完全以本表为准。

## 4. musl 结论

当前 140 个入口支撑固定 musl pthread consumer、动态 BusyBox、`dlopen`、multithreaded `posix_spawn/system/popen`、AF_UNIX epoll event-loop、AF_INET UDP/TCP/raw ICMP、AF_PACKET DHCP/DNS/HTTP、OpenSSL HTTPS consumer，以及真实 Alpine curl、SQLite、Git、htop 的固定竖切。验证仍不表示任意 musl 程序可运行；剩余缺口包括：

1. futex PI/PI-requeue/WAKE_OP 与完整 clone flags；
2. 其他 syscall 的 restart coverage、带 relative timeout futex 的正确剩余时间与 queued realtime signal；
3. `AT_HWCAP` 与 vDSO；
4. tmpfs/shmem pathname object与后台 writeback/reclaim worker。
5. IPv6、非 ICMP raw IP 与 ARP packet protocol、multicast、多 interface/network namespace，以及 TCP advanced option/urgent-data policy。

因此不能将固定 smoke 通过、编号一致或具有 Linux 格式 auxv 提升为通用 musl 兼容声明。
